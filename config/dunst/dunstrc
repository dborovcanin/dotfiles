
[global]
    # On Wayland, the compositor chooses the output for mouse/keyboard follow.
    follow = mouse
    # Invoke the notification's default action (e.g. open the Slack discussion).
    mouse_left_click = do_action, close_current
    indicate_hidden = yes
    # Hide the (A)/(U)/(AU) actions and URLs markers dunst prepends to summaries.
    # Purely cosmetic: mouse_left_click still triggers the default action.
    show_indicators = no
    separator_height = 2
    padding = 10
    horizontal_padding = 12
    frame_width = 3
    corner_radius = 10
    frame_color = "#dbc291"
    separator_color = "#504945"
    close = ctrl+space
    close_all = ctrl+shift+space
    history = mod1+grave
    context = ctrl+shift+period
    origin = top-center
    enable_recursive_icon_lookup = false
#   max_icon_size = 64
#   icon_position = off
    icon_theme = Papirus
    icon_path = /usr/share/icons/Papirus/16x16/status:/usr/share/icons/Papirus/16x16/devices:/usr/share/icons/hicolor/48x48/apps
    enable_recursive_icon_lookup = true


# Sort messages by urgency
    sort = yes
    gap_size = 10

    title = Dunst
    class = Dunst

    idle_threshold = 120
    font = DejaVu Sans Mono 14

[urgency_low]
    background = "#282828"
    foreground = "#a89984"
    timeout = 20

[urgency_normal]
    background = "#282828"
    foreground = "#ebdbb2"
    timeout = 20

[urgency_critical]
    background = "#1d2021"
    foreground = "#ebdbb2"
    frame_color = "#fb4934"
    timeout = 0

[slack]
    appname = Slack
    new_icon = slack
