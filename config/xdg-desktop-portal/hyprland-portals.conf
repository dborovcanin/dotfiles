[preferred]
# hyprland's portal does screen capture with window and region picking, and
# GlobalShortcuts; gtk does Settings (color-scheme), FileChooser and the rest.
# Needs xdg-desktop-portal-hyprland.
default=hyprland;gtk;
